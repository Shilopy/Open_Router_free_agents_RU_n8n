# 🤖 OpenRouter Free Experts RU (n8n Workflow)

Интеллектуальный трёхфазный тестировщик и ранжировщик бесплатных AI моделей через OpenRouter API для многоагентных систем. Оптимизирован для использования из России.

> 📄 Подробная инструкция для новичков: [ИНСТРУКЦИЯ.md](ИНСТРУКЦИЯ.md)

## 📋 Описание

Workflow `OpenRouter_Advanced_THREE_PHASE.json` автоматически находит, тестирует и ранжирует лучшие бесплатные AI модели в три фазы:

```
PHASE 0: Pre-Scoring     — фильтрация и скоринг по метаданным каталога
PHASE 1: Quick Test      — проверка доступности (быстрый пинг)
PHASE 2: Detailed Tests  — 4 специализированных теста качества
         └─ HTML Report  — дашборд с результатами (сохраняется на Рабочий стол)
```

### Что тестируется (5 тестов):
| Тест | Описание |
|------|----------|
| ✅ T1 Availability | Доступность модели |
| 🛡️ T2 Security | Анализ безопасности кода |
| ✨ T3 Code Quality | Качество генерации кода |
| 🧠 T4 Reasoning | Логическое мышление |
| 📚 T5 Documentation | Генерация документации |

### Итоговая формула скора:
```
FinalScore = PreScore × 0.40 + AvgQuality × 0.40 + TokenEfficiency × 0.20
```

## 🚀 Быстрый старт

### Требования:
- n8n (self-hosted или cloud)
- OpenRouter API ключ ([получить здесь](https://openrouter.ai/keys))

### Установка:
1. Импортируй `OpenRouter_Advanced_THREE_PHASE.json` в n8n
2. Открой узел `⚙️ Configuration`
3. Замени `YOUR_OPENROUTER_API_KEY_HERE` на свой реальный API ключ
4. Выбери режим задачи в `task_mode` (опционально)
5. Запусти workflow — HTML-отчёт появится на Рабочем столе

## ⚙️ Конфигурация

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `openrouter_api_key` | API ключ OpenRouter | *(обязательно)* |
| `task_mode` | Режим задачи | `universal` |
| `manual_models` | Список конкретных моделей для теста | *(пусто = авто)* |
| `phase0_max_models` | Моделей после Phase 0 | `30` |
| `phase1_max_models` | Моделей после Phase 1 | `15` |
| `phase2_max_models` | Моделей в финале | `10` |
| `rate_limit_ms` | Задержка между запросами (мс) | `2000` |
| `min_context_length` | Мин. контекст модели | `32000` |

### Режимы задач (`task_mode`):
| Режим | Назначение |
|-------|-----------|
| `universal` | Универсальный (по умолчанию) |
| `coding` | Упор на генерацию кода |
| `reasoning` | Логика и рассуждения |
| `chatbot` | Диалоговые ассистенты |
| `economy` | Самые дешёвые/быстрые |
| `power` | Максимальное качество |
| `speed` | Минимальная латентность |

### Ручной выбор моделей:
Укажи в `manual_models` ID через запятую, чтобы тестировать только их:
```
deepseek/deepseek-chat,meta-llama/llama-3.1-70b-instruct:free
```

## 📊 Результаты

После выполнения workflow:
- **`openrouter_report.html`** — HTML-дашборд сохраняется на Рабочий стол
- **JSON** — экспорт через встроенный N8N Convert to File

## 🇷🇺 Оптимизация для России

Phase 0 автоматически учитывает:
- Блокировку ряда западных моделей (GPT-4o, Claude-3 и др.)
- Бонус для провайдеров, стабильно работающих из РФ (DeepSeek, Qwen, Llama, Mistral, Gemma)

## 📝 Лицензия

MIT License

## 🤝 Автор

[@Shilopy](https://github.com/Shilopy)
